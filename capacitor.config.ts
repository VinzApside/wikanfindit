import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wikan-find-it',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
